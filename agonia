<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ОДНОКОМНАТНАЯ АГОНИЯ</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Press+Start+2P&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: #000;
            color: #0ff;
            font-family: 'Orbitron', sans-serif;
            overflow-x: hidden;
            position: relative;
            min-height: 100vh;
            cursor: url("data:image/svg+xml,%3Csvg width='24' height='24' viewBox='0 0 24 24' fill='%2300FFFF' xmlns='http://www.w3.org/2000/svg'%3E%3Ccircle cx='12' cy='12' r='6' stroke='%23FF00FF' stroke-width='2'/%3E%3C/svg%3E"), auto;
        }
        
        /* Анимированный фон */
        .background-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -10;
            background: 
                radial-gradient(circle at 20% 30%, rgba(255, 0, 255, 0.1) 0%, transparent 20%),
                radial-gradient(circle at 80% 70%, rgba(0, 255, 255, 0.1) 0%, transparent 20%),
                radial-gradient(circle at 40% 80%, rgba(255, 255, 0, 0.1) 0%, transparent 20%),
                linear-gradient(45deg, #000 0%, #111 100%);
            animation: backgroundPulse 15s infinite alternate;
        }
        
        @keyframes backgroundPulse {
            0% { filter: hue-rotate(0deg) brightness(0.8); }
            100% { filter: hue-rotate(90deg) brightness(1.1); }
        }
        
        /* Мерцающие частицы */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -5;
        }
        
        .particle {
            position: absolute;
            background-color: rgba(0, 255, 255, 0.7);
            border-radius: 50%;
            animation: floatParticle linear infinite;
        }
        
        @keyframes floatParticle {
            0% { transform: translateY(0) translateX(0); opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { transform: translateY(-100vh) translateX(20px); opacity: 0; }
        }
        
        /* Основной контейнер */
        .container {
            max-width: 100%;
            padding: 20px;
            position: relative;
            z-index: 1;
        }
        
        /* Заголовок */
        .header {
            text-align: center;
            padding: 30px 0;
            border-bottom: 3px solid #ff00ff;
            margin-bottom: 30px;
            text-shadow: 0 0 10px #ff00ff, 0 0 20px #ff00ff;
            animation: titleGlitch 5s infinite;
            position: relative;
            overflow: hidden;
        }
        
        .title {
            font-family: 'Press Start 2P', cursive;
            font-size: 3.5rem;
            color: #ff00ff;
            letter-spacing: 3px;
            margin-bottom: 15px;
            position: relative;
            display: inline-block;
        }
        
        .title::before {
            content: "ОДНОКОМНАТНАЯ АГОНИЯ";
            position: absolute;
            left: 2px;
            top: 2px;
            color: #00ffff;
            z-index: -1;
            animation: textFlicker 0.1s infinite;
        }
        
        @keyframes titleGlitch {
            0%, 100% { transform: translateX(0); }
            97% { transform: translateX(0); }
            98% { transform: translateX(-5px); }
            99% { transform: translateX(5px); }
        }
        
        @keyframes textFlicker {
            0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% { opacity: 1; }
            20%, 24%, 55% { opacity: 0.5; }
        }
        
        .subtitle {
            font-size: 1.2rem;
            color: #ffff00;
            text-shadow: 0 0 5px #ffff00;
            font-style: italic;
            animation: subtitlePulse 3s infinite alternate;
        }
        
        @keyframes subtitlePulse {
            0% { opacity: 0.7; transform: scale(0.98); }
            100% { opacity: 1; transform: scale(1.02); }
        }
        
        /* Основное игровое поле */
        .game-main {
            display: grid;
            grid-template-columns: 1fr 1.5fr;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        @media (max-width: 1200px) {
            .game-main {
                grid-template-columns: 1fr;
            }
        }
        
        /* Статистика */
        .stats-section {
            background: rgba(0, 0, 0, 0.8);
            border: 2px solid #00ffff;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
            animation: statBoxPulse 4s infinite alternate;
            position: relative;
            overflow: hidden;
        }
        
        .stats-section::before {
            content: "";
            position: absolute;
            top: -10px;
            left: -10px;
            right: -10px;
            bottom: -10px;
            background: linear-gradient(45deg, #ff00ff, #00ffff, #ffff00, #ff00ff);
            z-index: -1;
            filter: blur(20px);
            opacity: 0.3;
            animation: borderRotate 10s linear infinite;
        }
        
        @keyframes borderRotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        @keyframes statBoxPulse {
            0% { box-shadow: 0 0 20px rgba(0, 255, 255, 0.5); }
            50% { box-shadow: 0 0 30px rgba(255, 0, 255, 0.7); }
            100% { box-shadow: 0 0 20px rgba(255, 255, 0, 0.5); }
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
        }
        
        .stat-item {
            padding: 15px;
            border-radius: 8px;
            background: rgba(20, 20, 30, 0.9);
            border-left: 5px solid;
            animation: statItemPulse 2s infinite alternate;
            transition: transform 0.3s;
        }
        
        .stat-item:hover {
            transform: translateY(-5px);
        }
        
        .stat-mental {
            border-left-color: #ff00ff;
            box-shadow: 0 0 10px rgba(255, 0, 255, 0.5);
        }
        
        .stat-money {
            border-left-color: #ffff00;
            box-shadow: 0 0 10px rgba(255, 255, 0, 0.5);
        }
        
        .stat-debt {
            border-left-color: #ff0000;
            box-shadow: 0 0 10px rgba(255, 0, 0, 0.5);
        }
        
        .stat-fatigue {
            border-left-color: #00ffff;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
        }
        
        .stat-relationship {
            border-left-color: #00ff00;
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.5);
        }
        
        .stat-day {
            border-left-color: #ff8800;
            box-shadow: 0 0 10px rgba(255, 136, 0, 0.5);
            grid-column: span 2;
        }
        
        @keyframes statItemPulse {
            0% { transform: translateY(0); }
            100% { transform: translateY(-3px); }
        }
        
        .stat-label {
            font-size: 0.9rem;
            color: #aaa;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: 900;
            text-shadow: 0 0 10px currentColor;
        }
        
        .stat-value.mental-value { color: #ff00ff; }
        .stat-value.money-value { color: #ffff00; }
        .stat-value.debt-value { color: #ff0000; }
        .stat-value.fatigue-value { color: #00ffff; }
        .stat-value.relationship-value { color: #00ff00; }
        .stat-value.day-value { color: #ff8800; }
        
        /* Прогресс-бары */
        .progress-container {
            margin-top: 10px;
            height: 8px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
            overflow: hidden;
        }
        
        .progress-bar {
            height: 100%;
            transition: width 0.5s ease;
        }
        
        .mental-progress { background: linear-gradient(90deg, #ff00ff, #9900ff); }
        .money-progress { background: linear-gradient(90deg, #ffff00, #ff9900); }
        .debt-progress { background: linear-gradient(90deg, #ff0000, #990000); }
        .fatigue-progress { background: linear-gradient(90deg, #00ffff, #0099ff); }
        .relationship-progress { background: linear-gradient(90deg, #00ff00, #009900); }
        
        /* Область действий */
        .actions-section {
            background: rgba(0, 0, 0, 0.8);
            border: 2px solid #ffff00;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 0 20px rgba(255, 255, 0, 0.5);
            animation: actionBoxPulse 5s infinite alternate;
        }
        
        @keyframes actionBoxPulse {
            0% { box-shadow: 0 0 20px rgba(255, 255, 0, 0.5); }
            100% { box-shadow: 0 0 30px rgba(255, 0, 255, 0.7); }
        }
        
        .action-category {
            margin-bottom: 25px;
        }
        
        .category-title {
            color: #00ffff;
            font-size: 1.3rem;
            margin-bottom: 15px;
            padding-bottom: 8px;
            border-bottom: 2px solid #00ffff;
            text-shadow: 0 0 5px #00ffff;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 15px;
        }
        
        .action-btn {
            background: rgba(30, 30, 40, 0.9);
            border: 2px solid;
            color: #fff;
            padding: 15px;
            border-radius: 8px;
            font-family: 'Orbitron', sans-serif;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s;
            text-align: left;
            position: relative;
            overflow: hidden;
            animation: btnGlow 2s infinite alternate;
        }
        
        .action-btn::before {
            content: "";
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .action-btn:hover::before {
            left: 100%;
        }
        
        .action-btn:hover {
            transform: translateY(-5px) scale(1.05);
        }
        
        .action-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none !important;
        }
        
        .action-btn:disabled:hover::before {
            left: -100%;
        }
        
        .work-btn { border-color: #00ffff; color: #00ffff; }
        .freelance-btn { border-color: #ffff00; color: #ffff00; }
        .debt-btn { border-color: #ff0000; color: #ff0000; }
        .relationship-btn { border-color: #ff00ff; color: #ff00ff; }
        .mental-btn { border-color: #00ff00; color: #00ff00; }
        .risk-btn { border-color: #ff8800; color: #ff8800; }
        
        @keyframes btnGlow {
            0% { box-shadow: 0 0 5px currentColor; }
            100% { box-shadow: 0 0 15px currentColor; }
        }
        
        /* Лог событий */
        .log-section {
            background: rgba(0, 0, 0, 0.9);
            border: 2px solid #ff00ff;
            border-radius: 10px;
            padding: 25px;
            margin-top: 30px;
            box-shadow: 0 0 20px rgba(255, 0, 255, 0.5);
            animation: logBoxPulse 6s infinite alternate;
        }
        
        @keyframes logBoxPulse {
            0% { box-shadow: 0 0 20px rgba(255, 0, 255, 0.5); }
            100% { box-shadow: 0 0 30px rgba(0, 255, 255, 0.7); }
        }
        
        .log-title {
            color: #ff00ff;
            font-size: 1.5rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
            text-shadow: 0 0 5px #ff00ff;
        }
        
        .log-content {
            height: 200px;
            overflow-y: auto;
            padding: 15px;
            background: rgba(10, 10, 20, 0.8);
            border-radius: 5px;
            font-family: 'Courier New', monospace;
            line-height: 1.5;
        }
        
        .log-entry {
            margin-bottom: 10px;
            padding: 8px;
            border-left: 3px solid;
            animation: logEntryAppear 0.5s;
        }
        
        @keyframes logEntryAppear {
            0% { opacity: 0; transform: translateX(-20px); }
            100% { opacity: 1; transform: translateX(0); }
        }
        
        .log-day { border-left-color: #ffff00; }
        .log-event { border-left-color: #ff0000; }
        .log-positive { border-left-color: #00ff00; }
        .log-warning { border-left-color: #ff8800; }
        
        /* Новые фичи */
        .features-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-top: 40px;
        }
        
        .feature-box {
            background: rgba(0, 0, 0, 0.8);
            border: 2px solid;
            border-radius: 10px;
            padding: 20px;
            animation: featureBoxFloat 3s infinite alternate;
        }
        
        @keyframes featureBoxFloat {
            0% { transform: translateY(0); }
            100% { transform: translateY(-10px); }
        }
        
        .feature-title {
            font-size: 1.3rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        /* Фича: Коллекторы */
        .collector-feature {
            border-color: #ff0000;
        }
        
        .collector-bar {
            height: 20px;
            background: rgba(255, 0, 0, 0.2);
            border-radius: 10px;
            margin-top: 10px;
            overflow: hidden;
        }
        
        .collector-progress {
            height: 100%;
            background: linear-gradient(90deg, #ff0000, #ff5555);
            width: 0%;
            transition: width 0.5s;
            border-radius: 10px;
        }
        
        /* Фича: Неоновые вывески */
        .neon-feature {
            border-color: #00ffff;
        }
        
        .neon-sign {
            font-family: 'Press Start 2P', cursive;
            color: #00ffff;
            text-shadow: 0 0 10px #00ffff;
            text-align: center;
            padding: 15px;
            background: rgba(0, 0, 20, 0.8);
            border-radius: 5px;
            margin-top: 10px;
            animation: neonFlicker 1s infinite alternate;
        }
        
        @keyframes neonFlicker {
            0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% { opacity: 1; }
            20%, 24%, 55% { opacity: 0.7; }
        }
        
        /* Фича: Мебель */
        .furniture-feature {
            border-color: #ffff00;
        }
        
        .furniture-progress {
            height: 10px;
            background: rgba(255, 255, 0, 0.2);
            border-radius: 5px;
            margin-top: 15px;
            overflow: hidden;
        }
        
        .furniture-bar {
            height: 100%;
            background: linear-gradient(90deg, #ffff00, #ffaa00);
            width: 0%;
            transition: width 0.5s;
            border-radius: 5px;
        }
        
        /* Экран концовки */
        .ending-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            z-index: 100;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.5s, visibility 0.5s;
            padding: 20px;
        }
        
        .ending-screen.active {
            opacity: 1;
            visibility: visible;
        }
        
        .ending-content {
            background: rgba(0, 0, 0, 0.9);
            border: 3px solid #ff00ff;
            border-radius: 15px;
            padding: 40px;
            max-width: 800px;
            width: 100%;
            text-align: center;
            box-shadow: 0 0 50px rgba(255, 0, 255, 0.8);
            animation: endingGlitch 3s infinite;
        }
        
        @keyframes endingGlitch {
            0%, 100% { transform: translateX(0); }
            97% { transform: translateX(0); }
            98% { transform: translateX(-10px); }
            99% { transform: translateX(10px); }
        }
        
        .ending-title {
            font-family: 'Press Start 2P', cursive;
            font-size: 2.5rem;
            color: #ff0000;
            margin-bottom: 30px;
            text-shadow: 0 0 20px #ff0000;
        }
        
        .ending-text {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 30px;
            color: #00ffff;
        }
        
        .restart-btn {
            background: transparent;
            border: 2px solid #00ff00;
            color: #00ff00;
            padding: 15px 30px;
            font-family: 'Orbitron', sans-serif;
            font-size: 1.2rem;
            cursor: pointer;
            border-radius: 8px;
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .restart-btn:hover {
            background: rgba(0, 255, 0, 0.2);
            box-shadow: 0 0 20px #00ff00;
            transform: scale(1.05);
        }
        
        /* Аудио контроль */
        .audio-control {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 50;
        }
        
        .audio-btn {
            background: rgba(0, 0, 0, 0.8);
            border: 2px solid #ffff00;
            color: #ffff00;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            font-size: 1.5rem;
            transition: all 0.3s;
        }
        
        .audio-btn:hover {
            transform: scale(1.1);
            box-shadow: 0 0 15px #ffff00;
        }
        
        /* Адаптивность */
        @media (max-width: 768px) {
            .title { font-size: 2rem; }
            .game-main { grid-template-columns: 1fr; }
            .stats-grid { grid-template-columns: 1fr; }
            .actions-grid { grid-template-columns: 1fr; }
            .features-section { grid-template-columns: 1fr; }
            .stat-item { grid-column: span 1 !important; }
        }
        
        /* Анимация загрузки */
        .loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #000;
            z-index: 1000;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            transition: opacity 1s, visibility 1s;
        }
        
        .loader.hidden {
            opacity: 0;
            visibility: hidden;
        }
        
        .loader-text {
            color: #00ffff;
            font-size: 1.5rem;
            margin-top: 20px;
            text-shadow: 0 0 10px #00ffff;
            animation: loaderText 2s infinite;
        }
        
        @keyframes loaderText {
            0% { opacity: 0.5; }
            50% { opacity: 1; }
            100% { opacity: 0.5; }
        }
        
        .spinner {
            width: 80px;
            height: 80px;
            border: 8px solid transparent;
            border-top: 8px solid #ff00ff;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <!-- Анимированный фон -->
    <div class="background-animation"></div>
    <div class="particles" id="particles-container"></div>
    
    <!-- Загрузчик -->
    <div class="loader" id="loader">
        <div class="spinner"></div>
        <div class="loader-text">ЗАГРУЗКА ОДНОКОМНАТНОЙ АГОНИИ...</div>
    </div>
    
    <!-- Основной контейнер -->
    <div class="container">
        <!-- Заголовок -->
        <header class="header">
            <h1 class="title">ОДНОКОМНАТНАЯ АГОНИЯ</h1>
            <p class="subtitle">Кислотный симулятор жизни на грани нервного срыва</p>
        </header>
        
        <!-- Основное игровое поле -->
        <div class="game-main">
            <!-- Статистика -->
            <section class="stats-section">
                <h2 class="category-title"><i class="fas fa-chart-line"></i> СОСТОЯНИЕ РОСТИСЛАВА</h2>
                <div class="stats-grid">
                    <div class="stat-item stat-mental">
                        <div class="stat-label"><i class="fas fa-brain"></i> ПСИХИКА</div>
                        <div class="stat-value mental-value" id="mental-value">50</div>
                        <div class="progress-container">
                            <div class="progress-bar mental-progress" id="mental-progress"></div>
                        </div>
                    </div>
                    
                    <div class="stat-item stat-money">
                        <div class="stat-label"><i class="fas fa-money-bill-wave"></i> ДЕНЬГИ</div>
                        <div class="stat-value money-value" id="money-value">10 000 ₽</div>
                        <div class="progress-container">
                            <div class="progress-bar money-progress" id="money-progress"></div>
                        </div>
                    </div>
                    
                    <div class="stat-item stat-debt">
                        <div class="stat-label"><i class="fas fa-credit-card"></i> ДОЛГ</div>
                        <div class="stat-value debt-value" id="debt-value">1 000 000 ₽</div>
                        <div class="progress-container">
                            <div class="progress-bar debt-progress" id="debt-progress"></div>
                        </div>
                    </div>
                    
                    <div class="stat-item stat-fatigue">
                        <div class="stat-label"><i class="fas fa-tired"></i> УСТАЛОСТЬ</div>
                        <div class="stat-value fatigue-value" id="fatigue-value">30</div>
                        <div class="progress-container">
                            <div class="progress-bar fatigue-progress" id="fatigue-progress"></div>
                        </div>
                    </div>
                    
                    <div class="stat-item stat-relationship">
                        <div class="stat-label"><i class="fas fa-heart"></i> ОТНОШЕНИЯ</div>
                        <div class="stat-value relationship-value" id="relationship-value">60</div>
                        <div class="progress-container">
                            <div class="progress-bar relationship-progress" id="relationship-progress"></div>
                        </div>
                    </div>
                    
                    <div class="stat-item stat-day">
                        <div class="stat-label"><i class="fas fa-calendar-day"></i> ДЕНЬ</div>
                        <div class="stat-value day-value" id="day-value">1</div>
                    </div>
                </div>
            </section>
            
            <!-- Действия -->
            <section class="actions-section">
                <div class="action-category">
                    <h3 class="category-title"><i class="fas fa-briefcase"></i> РАБОТА</h3>
                    <div class="actions-grid" id="work-actions">
                        <!-- Заполняется JS -->
                    </div>
                </div>
                
                <div class="action-category">
                    <h3 class="category-title"><i class="fas fa-tools"></i> ФРИЛАНС</h3>
                    <div class="actions-grid" id="freelance-actions">
                        <!-- Заполняется JS -->
                    </div>
                </div>
                
                <div class="action-category">
                    <h3 class="category-title"><i class="fas fa-balance-scale"></i> ФИНАНСЫ</h3>
                    <div class="actions-grid" id="finance-actions">
                        <!-- Заполняется JS -->
                    </div>
                </div>
                
                <div class="action-category">
                    <h3 class="category-title"><i class="fas fa-user-friends"></i> ОТНОШЕНИЯ</h3>
                    <div class="actions-grid" id="relationship-actions">
                        <!-- Заполняется JS -->
                    </div>
                </div>
                
                <div class="action-category">
                    <h3 class="category-title"><i class="fas fa-first-aid"></i> ПСИХИКА</h3>
                    <div class="actions-grid" id="mental-actions">
                        <!-- Заполняется JS -->
                    </div>
                </div>
            </section>
        </div>
        
        <!-- Лог событий -->
        <section class="log-section">
            <h2 class="log-title"><i class="fas fa-scroll"></i> ЛОГ АГОНИИ</h2>
            <div class="log-content" id="log-content">
                <!-- Заполняется JS -->
            </div>
        </section>
        
        <!-- Новые фичи -->
        <div class="features-section">
            <!-- Коллекторы -->
            <div class="feature-box collector-feature">
                <h3 class="feature-title"><i class="fas fa-phone-volume"></i> КОЛЛЕКТОРЫ</h3>
                <p id="collector-status">Пока тихо... Но они уже знают о ваших долгах.</p>
                <div class="collector-bar">
                    <div class="collector-progress" id="collector-progress"></div>
                </div>
                <p id="collector-timer">До звонка: <span id="collector-countdown">5</span> дней</p>
            </div>
            
            <!-- Неоновые вывески -->
            <div class="feature-box neon-feature">
                <h3 class="feature-title"><i class="fas fa-sign"></i> НЕОНОВЫЕ ВЫВЕСКИ</h3>
                <p>Ваша основная работа. Сегодня нужно собрать:</p>
                <div class="neon-sign" id="neon-sign-text">"СЧАСТЬЕ ЕСТЬ"</div>
                <p id="neon-bonus">Бонус за качество: 0 ₽</p>
            </div>
            
            <!-- Мебель -->
            <div class="feature-box furniture-feature">
                <h3 class="feature-title"><i class="fas fa-couch"></i> СБОРКА МЕБЕЛИ</h3>
                <p id="furniture-task">Текущий заказ: Нет</p>
                <div class="furniture-progress">
                    <div class="furniture-bar" id="furniture-bar"></div>
                </div>
                <p id="furniture-reward">Награда: 0 ₽</p>
            </div>
        </div>
    </div>
    
    <!-- Аудио контроль -->
    <div class="audio-control">
        <div class="audio-btn" id="audio-btn">
            <i class="fas fa-volume-up"></i>
        </div>
    </div>
    
    <!-- Экран концовки -->
    <div class="ending-screen" id="ending-screen">
        <div class="ending-content">
            <h2 class="ending-title" id="ending-title">КОНЕЦ ИГРЫ</h2>
            <div class="ending-text" id="ending-text"></div>
            <button class="restart-btn" id="restart-btn">НАЧАТЬ АГОНИЮ СНАЧАЛА</button>
        </div>
    </div>

    <!-- Аудио элементы -->
    <audio id="bg-music" loop>
        <source src="https://assets.mixkit.co/music/preview/mixkit-tech-house-vibes-130.mp3" type="audio/mpeg">
    </audio>
    
    <audio id="click-sound">
        <source src="https://assets.mixkit.co/sfx/preview/mixkit-select-click-1109.mp3" type="audio/mpeg">
    </audio>
    
    <audio id="money-sound">
        <source src="https://assets.mixkit.co/sfx/preview/mixkit-cash-register-bell-sale-1075.mp3" type="audio/mpeg">
    </audio>
    
    <audio id="negative-sound">
        <source src="https://assets.mixkit.co/sfx/preview/mixkit-wrong-answer-fail-notification-946.mp3" type="audio/mpeg">
    </audio>

    <script>
        // Игровое состояние
        const gameState = {
            day: 1,
            money: 10000,
            debt: 1000000,
            fatigue: 30,
            mentalHealth: 50,
            relationship: 60,
            isGameOver: false,
            
            // Новые параметры
            collectorPressure: 0,
            daysToCollectorCall: 5,
            neonBonus: 0,
            furnitureProgress: 0,
            furnitureReward: 0,
            furnitureActive: false,
            
            // Константы
            SALARY: 100000,
            RENT: 30000,
            MIN_DEBT_PAYMENT: 15000,
            FREELANCE_CHANCE: 0.4,
            FREELANCE_REWARD: 15000,
            OVERTIME_PAY: 20000,
            WORK_FATIGUE: 15,
            FURNITURE_FATIGUE: 20,
            FURNITURE_PAY: 8000,
            NEW_LOAN_AMOUNT: 150000,
            THERAPY_COST: 10000,
            THERAPY_MENTAL_BOOST: 15,
            DATE_COST: 5000,
            DATE_RELATIONSHIP_BOOST: 10,
            
            // Лог событий
            log: [],
            
            // Случайные события
            randomEvents: [
                {
                    name: "Неожиданная трата",
                    chance: 0.2,
                    message: "Сломался унитаз. Пришлось вызвать сантехника и заплатить 8000 ₽.",
                    effect: {money: -8000, mentalHealth: -5}
                },
                {
                    name: "Отмена заказа",
                    chance: 0.15,
                    message: "Клиент отменил заказ на сборку мебели в последний момент.",
                    effect: {mentalHealth: -10, fatigue: 5}
                },
                {
                    name: "Разговор по душам",
                    chance: 0.25,
                    message: "Девушка хочет 'поговорить по душам'. После трёх часов разговора о ваших недостатках вы чувствуете себя хуже.",
                    effect: {mentalHealth: -15, relationship: 5}
                },
                {
                    name: "Штраф",
                    chance: 0.1,
                    message: "Пришёл штраф за просроченный платёж по кредиту: 5000 ₽.",
                    effect: {money: -5000, mentalHealth: -8}
                },
                {
                    name: "Удача",
                    chance: 0.1,
                    message: "Вы нашли на улице 2000 ₽. Не спрашивайте, почему они были в лужe.",
                    effect: {money: 2000, mentalHealth: 3}
                },
                {
                    name: "Подарок от девушки",
                    chance: 0.05,
                    message: "Девушка неожиданно подарила вам носки. Это немного улучшило настроение.",
                    effect: {mentalHealth: 5, relationship: 3}
                },
                {
                    name: "Коллекторы",
                    chance: 0.3,
                    message: "Звонок от коллекторов. Они интересуются, когда вы планируете вернуть долг.",
                    effect: {mentalHealth: -10, collectorPressure: 20}
                }
            ],
            
            // Неоновые вывески
            neonSigns: [
                "СЧАСТЬЕ ЕСТЬ",
                "ДЕНЬГИ НЕ ПАХНУТ",
                "ВСЁ ПРОЙДЁТ",
                "КРЕДИТ НАВСЕГДА",
                "РАБОТА СПАСЁТ",
                "ЛЮБОВЬ СЛЕПА",
                "ДОЛГ ЧЕСТЬЮ",
                "НЕ ПЛАЧЬ",
                "ЗАВТРА ЛУЧШЕ",
                "НЕТ ВЫХОДА"
            ],
            
            // Типы мебели
            furnitureTypes: [
                {name: "ШКАФ ИКЕА", time: 3, reward: 12000},
                {name: "ДИВАН", time: 4, reward: 15000},
                {name: "СТОЛ КОМПЬЮТЕРНЫЙ", time: 2, reward: 8000},
                {name: "КУХОННЫЙ ГАРНИТУР", time: 5, reward: 20000},
                {name: "КНИЖНЫЙ ШКАФ", time: 3, reward: 10000}
            ],
            
            // Концовки
            endings: [
                {
                    id: "mental_breakdown",
                    title: "ПСИХИЧЕСКИЙ СРЫВ",
                    condition: () => gameState.mentalHealth <= 0,
                    text: "После {days} дней непрерывного давления ваша психика не выдержала. Вы потеряли работу, девушка ушла, а долги остались. Теперь вы живёте на улице, иногда вспоминая неоновые вывески, которые когда-то собирали."
                },
                {
                    id: "debt_spiral",
                    title: "ВЕЧНАЯ ДОЛГОВАЯ ПЕТЛЯ",
                    condition: () => gameState.debt >= 2000000,
                    text: "Ваш долг превысил 2 000 000 ₽. Кредиторы забрали всё, включая однокомнатную квартиру. Вы стали вечным должником, обречённым работать на погашение процентов."
                },
                {
                    id: "relationship_end",
                    title: "ОДИНОЧЕСТВО В КЛЕТКЕ",
                    condition: () => gameState.relationship <= 0,
                    text: "Девушка ушла, не выдержав постоянных финансовых проблем. Вы остались один в этой однокомнатной квартире с долгом в {debt} ₽."
                },
                {
                    id: "financial_salvation",
                    title: "ФИНАНСОВОЕ СПАСЕНИЕ, ПСИХИЧЕСКИЙ КРАХ",
                    condition: () => gameState.debt <= 0 && gameState.mentalHealth <= 20,
                    text: "Вы выплатили все долги после {days} дней нечеловеческих усилий. Но цена оказалась слишком высокой - ваша психика разрушена."
                },
                {
                    id: "almost_happy",
                    title: "КОМПРОМИСС С ЖИЗНЬЮ",
                    condition: () => gameState.debt <= 0 && gameState.mentalHealth >= 60 && gameState.relationship >= 70,
                    text: "После {days} дней борьбы вам удалось выплатить долги. Отношения сохранились, хотя и дали трещину. Это не счастье, но и не ад. Просто жизнь."
                },
                {
                    id: "burnout",
                    title: "СГОРЕЛ НА РАБОТЕ",
                    condition: () => gameState.fatigue >= 100,
                    text: "Ваш организм не выдержал бесконечных переработок. После {days} дней без отдыха сердце остановилось."
                },
                {
                    id: "collector_takeover",
                    title: "КОЛЛЕКТОРЫ ЗАБРАЛИ ВСЁ",
                    condition: () => gameState.collectorPressure >= 100,
                    text: "Коллекторы пришли лично. Они забрали всё ценное, включая ваше достоинство. Теперь вы работаете на них, чтобы выплачивать долги."
                }
            ]
        };

        // Инициализация игры
        function initGame() {
            // Скрыть загрузчик
            setTimeout(() => {
                document.getElementById('loader').classList.add('hidden');
                playSound('click-sound');
            }, 2000);
            
            // Создать частицы
            createParticles();
            
            // Настроить аудио
            initAudio();
            
            // Инициализировать UI
            updateUI();
            generateActions();
            updateLog("День 1. Вы просыпаетесь в своей однокомнатной квартире. Рядом храпит ваша девушка. На столе лежат счета за коммуналку и напоминания о долгах.", "day");
            
            // Обновить неоновую вывеску
            updateNeonSign();
            
            // Запустить игровой цикл
            setInterval(gameLoop, 100);
        }
        
        // Создать частицы
        function createParticles() {
            const container = document.getElementById('particles-container');
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Случайный размер и позиция
                const size = Math.random() * 5 + 2;
                particle.style.width = `${size}px`;
                particle.style.height = `${size}px`;
                particle.style.left = `${Math.random() * 100}%`;
                particle.style.backgroundColor = `hsl(${Math.random() * 360}, 100%, 50%)`;
                
                // Случайная анимация
                const duration = Math.random() * 20 + 10;
                const delay = Math.random() * 20;
                particle.style.animationDuration = `${duration}s`;
                particle.style.animationDelay = `${delay}s`;
                
                container.appendChild(particle);
            }
        }
        
        // Инициализация аудио
        function initAudio() {
            const audioBtn = document.getElementById('audio-btn');
            const bgMusic = document.getElementById('bg-music');
            
            // Попытка воспроизведения музыки
            bgMusic.volume = 0.3;
            
            audioBtn.addEventListener('click', () => {
                if (bgMusic.paused) {
                    bgMusic.play();
                    audioBtn.innerHTML = '<i class="fas fa-volume-up"></i>';
                    playSound('click-sound');
                } else {
                    bgMusic.pause();
                    audioBtn.innerHTML = '<i class="fas fa-volume-mute"></i>';
                    playSound('click-sound');
                }
            });
            
            // Автовоспроизведение с взаимодействия пользователя
            document.addEventListener('click', () => {
                if (bgMusic.paused) {
                    bgMusic.play().catch(e => console.log("Автовоспроизведение заблокировано"));
                }
            }, { once: true });
        }
        
        // Воспроизведение звука
        function playSound(soundId) {
            const sound = document.getElementById(soundId);
            if (sound) {
                sound.currentTime = 0;
                sound.play().catch(e => console.log("Звук не воспроизведен"));
            }
        }
        
        // Игровой цикл
        function gameLoop() {
            if (gameState.isGameOver) return;
            
            // Обновление коллекторов
            updateCollectors();
            
            // Обновление прогресса мебели
            updateFurniture();
            
            // Проверка концовок
            checkEndings();
        }
        
        // Обновление UI
        function updateUI() {
            // Обновление значений
            document.getElementById('mental-value').textContent = gameState.mentalHealth;
            document.getElementById('money-value').textContent = formatMoney(gameState.money) + " ₽";
            document.getElementById('debt-value').textContent = formatMoney(gameState.debt) + " ₽";
            document.getElementById('fatigue-value').textContent = gameState.fatigue;
            document.getElementById('relationship-value').textContent = gameState.relationship;
            document.getElementById('day-value').textContent = gameState.day;
            
            // Обновление прогресс-баров
            document.getElementById('mental-progress').style.width = `${gameState.mentalHealth}%`;
            document.getElementById('money-progress').style.width = `${Math.min(100, gameState.money / 50000 * 100)}%`;
            document.getElementById('debt-progress').style.width = `${Math.min(100, gameState.debt / 2000000 * 100)}%`;
            document.getElementById('fatigue-progress').style.width = `${gameState.fatigue}%`;
            document.getElementById('relationship-progress').style.width = `${gameState.relationship}%`;
            
            // Обновление коллекторов
            document.getElementById('collector-progress').style.width = `${gameState.collectorPressure}%`;
            document.getElementById('collector-countdown').textContent = gameState.daysToCollectorCall;
            
            // Обновление статуса коллекторов
            let collectorStatus = document.getElementById('collector-status');
            if (gameState.collectorPressure < 30) {
                collectorStatus.textContent = "Пока тихо... Но они уже знают о ваших долгах.";
            } else if (gameState.collectorPressure < 60) {
                collectorStatus.textContent = "Коллекторы начали звонить. Они вежливы, но настойчивы.";
            } else if (gameState.collectorPressure < 90) {
                collectorStatus.textContent = "Коллекторы угрожают. Ваша психика на пределе.";
            } else {
                collectorStatus.textContent = "Коллекторы скоро приедут лично. Бегите.";
            }
            
            // Обновление неонового бонуса
            document.getElementById('neon-bonus').textContent = `Бонус за качество: ${formatMoney(gameState.neonBonus)} ₽`;
            
            // Обновление мебели
            if (gameState.furnitureActive) {
                document.getElementById('furniture-bar').style.width = `${gameState.furnitureProgress}%`;
            }
        }
        
        // Форматирование денег
        function formatMoney(amount) {
            return amount.toString().replace(/\B(?=(\d{3})+(?!\d))/g, " ");
        }
        
        // Обновление лога
        function updateLog(message, type = "event") {
            const logContent = document.getElementById('log-content');
            const logEntry = document.createElement('div');
            
            logEntry.classList.add('log-entry', `log-${type}`);
            logEntry.innerHTML = `<strong>День ${gameState.day}:</strong> ${message}`;
            
            logContent.appendChild(logEntry);
            logContent.scrollTop = logContent.scrollHeight;
            
            // Сохранить в истории
            gameState.log.push({day: gameState.day, message, type});
            
            // Ограничить размер лога
            if (gameState.log.length > 20) {
                gameState.log.shift();
                if (logContent.children.length > 0) {
                    logContent.removeChild(logContent.children[0]);
                }
            }
        }
        
        // Генерация действий
        function generateActions() {
            // Очистка контейнеров
            const containers = ['work-actions', 'freelance-actions', 'finance-actions', 'relationship-actions', 'mental-actions'];
            containers.forEach(id => {
                document.getElementById(id).innerHTML = '';
            });
            
            // Работа
            const workActions = [
                { name: "Пойти на работу", action: goToWork, cost: 0, className: "work-btn", disabled: false },
                { name: "Взять переработку", action: takeOvertime, cost: 0, className: "work-btn", disabled: gameState.fatigue > 70 }
            ];
            
            // Фриланс
            const freelanceActions = [
                { name: "Искать фриланс", action: searchFreelance, cost: 0, className: "freelance-btn", disabled: false },
                { name: "Собрать мебель", action: startFurniture, cost: 0, className: "freelance-btn", disabled: gameState.furnitureActive || gameState.fatigue > 80 }
            ];
            
            // Финансы
            const financeActions = [
                { name: "Взять новый кредит", action: takeNewLoan, cost: 0, className: "debt-btn", disabled: false },
                { name: "Заплатить за квартиру", action: payRent, cost: gameState.RENT, className: "debt-btn", disabled: gameState.money < gameState.RENT },
                { name: "Внести платеж по долгу", action: payDebt, cost: gameState.MIN_DEBT_PAYMENT, className: "debt-btn", disabled: gameState.money < gameState.MIN_DEBT_PAYMENT }
            ];
            
            // Отношения
            const relationshipActions = [
                { name: "Проигнорировать просьбу", action: ignoreRequest, cost: 0, className: "relationship-btn", disabled: false },
                { name: "Подчиниться просьбе", action: complyWithRequest, cost: gameState.DATE_COST, className: "relationship-btn", disabled: gameState.money < gameState.DATE_COST },
                { name: "Устроить скандал", action: haveFight, cost: 0, className: "risk-btn", disabled: false }
            ];
            
            // Психика
            const mentalActions = [
                { name: "Восстановить психику", action: restoreMentalHealth, cost: gameState.THERAPY_COST, className: "mental-btn", disabled: gameState.money < gameState.THERAPY_COST },
                { name: "Выпить энергетик", action: drinkEnergy, cost: 200, className: "mental-btn", disabled: gameState.money < 200 },
                { name: "Оформить банкротство", action: declareBankruptcy, cost: 0, className: "risk-btn", disabled: false }
            ];
            
            // Создание кнопок
            createActionButtons('work-actions', workActions);
            createActionButtons('freelance-actions', freelanceActions);
            createActionButtons('finance-actions', financeActions);
            createActionButtons('relationship-actions', relationshipActions);
            createActionButtons('mental-actions', mentalActions);
        }
        
        // Создание кнопок действий
        function createActionButtons(containerId, actions) {
            const container = document.getElementById(containerId);
            
            actions.forEach(actionData => {
                const button = document.createElement('button');
                button.className = `action-btn ${actionData.className}`;
                button.textContent = actionData.name;
                button.disabled = actionData.disabled || gameState.isGameOver;
                
                // Добавить стоимость, если есть
                if (actionData.cost > 0) {
                    button.title = `Стоимость: ${formatMoney(actionData.cost)} ₽`;
                }
                
                button.addEventListener('click', () => {
                    if (!gameState.isGameOver) {
                        actionData.action();
                        playSound('click-sound');
                    }
                });
                
                container.appendChild(button);
            });
        }
        
        // Действия игрока
        function goToWork() {
            const salary = gameState.SALARY + gameState.neonBonus;
            gameState.money += salary;
            gameState.fatigue += gameState.WORK_FATIGUE;
            gameState.neonBonus = 0; // Сброс бонуса
            
            updateLog(`Вы отработали день, собирая неоновые вывески. Получено: ${formatMoney(salary)} ₽. Усталость: +${gameState.WORK_FATIGUE}.`, "positive");
            
            // Шанс бонуса за качество
            if (Math.random() < 0.3 && gameState.mentalHealth > 30) {
                gameState.neonBonus = Math.floor(Math.random() * 20000) + 5000;
                updateLog(`Начальник оценил качество вашей работы! Бонус: ${formatMoney(gameState.neonBonus)} ₽`, "positive");
                playSound('money-sound');
            }
            
            processDayEnd();
        }
        
        function takeOvertime() {
            gameState.money += gameState.OVERTIME_PAY;
            gameState.fatigue += 25;
            gameState.mentalHealth -= 8;
            
            updateLog(`Вы остались на переработку. Получено: ${formatMoney(gameState.OVERTIME_PAY)} ₽. Усталость: +25. Психика: -8.`, "warning");
            processDayEnd();
        }
        
        function searchFreelance() {
            if (Math.random() < gameState.FREELANCE_CHANCE) {
                gameState.money += gameState.FREELANCE_REWARD;
                updateLog(`Вам повезло! Вы нашли заказ на сборку мебели. Получено: ${formatMoney(gameState.FREELANCE_REWARD)} ₽.`, "positive");
                playSound('money-sound');
            } else {
                updateLog("Вы потратили весь день на поиски заказов, но ничего не нашли.", "warning");
                gameState.fatigue += 10;
                gameState.mentalHealth -= 5;
            }
            
            processDayEnd();
        }
        
        function startFurniture() {
            const furniture = gameState.furnitureTypes[Math.floor(Math.random() * gameState.furnitureTypes.length)];
            gameState.furnitureActive = true;
            gameState.furnitureProgress = 0;
            gameState.furnitureReward = furniture.reward;
            
            updateLog(`Вы начали сборку мебели: ${furniture.name}. Награда: ${formatMoney(furniture.reward)} ₽.`, "positive");
            document.getElementById('furniture-task').textContent = `Текущий заказ: ${furniture.name}`;
            document.getElementById('furniture-reward').textContent = `Награда: ${formatMoney(furniture.reward)} ₽`;
            
            processDayEnd();
        }
        
        function updateFurniture() {
            if (gameState.furnitureActive && gameState.furnitureProgress < 100) {
                gameState.furnitureProgress += 0.5;
                document.getElementById('furniture-bar').style.width = `${gameState.furnitureProgress}%`;
                
                if (gameState.furnitureProgress >= 100) {
                    completeFurniture();
                }
            }
        }
        
        function completeFurniture() {
            gameState.money += gameState.furnitureReward;
            gameState.fatigue += gameState.FURNITURE_FATIGUE;
            gameState.furnitureActive = false;
            
            updateLog(`Вы завершили сборку мебели! Получено: ${formatMoney(gameState.furnitureReward)} ₽. Усталость: +${gameState.FURNITURE_FATIGUE}.`, "positive");
            playSound('money-sound');
            
            document.getElementById('furniture-task').textContent = "Текущий заказ: Нет";
            document.getElementById('furniture-reward').textContent = "Награда: 0 ₽";
            document.getElementById('furniture-bar').style.width = "0%";
        }
        
        function takeNewLoan() {
            const loanAmount = gameState.NEW_LOAN_AMOUNT;
            const interest = Math.floor(loanAmount * 0.2);
            const totalDebt = loanAmount + interest;
            
            gameState.money += loanAmount;
            gameState.debt += totalDebt;
            gameState.mentalHealth -= 10;
            gameState.collectorPressure += 15;
            
            updateLog(`Вы взяли новый кредит: ${formatMoney(loanAmount)} ₽. С процентами долг увеличился на ${formatMoney(totalDebt)} ₽. Психика: -10.`, "warning");
            playSound('money-sound');
            processDayEnd();
        }
        
        function payRent() {
            if (gameState.money >= gameState.RENT) {
                gameState.money -= gameState.RENT;
                gameState.relationship += 5;
                updateLog(`Вы оплатили аренду квартиры: ${formatMoney(gameState.RENT)} ₽. Отношения: +5.`, "positive");
            } else {
                updateLog("У вас недостаточно денег для оплаты аренды. Девушка недовольна.", "warning");
                gameState.relationship -= 15;
                gameState.mentalHealth -= 10;
                playSound('negative-sound');
            }
            
            processDayEnd();
        }
        
        function payDebt() {
            if (gameState.money >= gameState.MIN_DEBT_PAYMENT) {
                gameState.money -= gameState.MIN_DEBT_PAYMENT;
                gameState.debt -= gameState.MIN_DEBT_PAYMENT;
                gameState.mentalHealth += 5;
                gameState.collectorPressure -= 10;
                
                if (gameState.collectorPressure < 0) gameState.collectorPressure = 0;
                
                updateLog(`Вы внесли платеж по долгу: ${formatMoney(gameState.MIN_DEBT_PAYMENT)} ₽. Долг уменьшен. Психика: +5.`, "positive");
                playSound('money-sound');
            } else {
                updateLog("У вас недостаточно денег для платежа по долгу.", "warning");
                playSound('negative-sound');
            }
            
            processDayEnd();
        }
        
        function ignoreRequest() {
            gameState.relationship -= 10;
            gameState.mentalHealth += 5;
            
            updateLog("Вы проигнорировали просьбу девушки сходить в дорогой ресторан. Отношения: -10. Психика: +5.", "warning");
            processDayEnd();
        }
        
        function complyWithRequest() {
            const cost = gameState.DATE_COST + Math.floor(Math.random() * 5000);
            gameState.money -= cost;
            gameState.relationship += gameState.DATE_RELATIONSHIP_BOOST;
            gameState.mentalHealth -= 8;
            gameState.fatigue += 5;
            
            updateLog(`Вы повели девушку в модное кафе. Потрачено: ${formatMoney(cost)} ₽. Отношения: +${gameState.DATE_RELATIONSHIP_BOOST}. Психика: -8.`, "warning");
            processDayEnd();
        }
        
        function haveFight() {
            const relationshipLoss = Math.floor(Math.random() * 20) + 10;
            const mentalGain = Math.floor(Math.random() * 15) + 5;
            
            gameState.relationship -= relationshipLoss;
            gameState.mentalHealth += mentalGain;
            
            updateLog(`Вы устроили скандал! Выплеснули все накопленное раздражение. Отношения: -${relationshipLoss}. Психика: +${mentalGain}.`, "warning");
            processDayEnd();
        }
        
        function restoreMentalHealth() {
            if (gameState.money >= gameState.THERAPY_COST) {
                gameState.money -= gameState.THERAPY_COST;
                gameState.mentalHealth += gameState.THERAPY_MENTAL_BOOST;
                
                updateLog(`Вы сходили к психологу. Потрачено: ${formatMoney(gameState.THERAPY_COST)} ₽. Психика: +${gameState.THERAPY_MENTAL_BOOST}.`, "positive");
                
                // Шанс, что терапия не поможет
                if (Math.random() < 0.2) {
                    updateLog("Психолог оказался шарлатаном. Вы почувствовали себя ещё хуже.", "warning");
                    gameState.mentalHealth -= 10;
                    playSound('negative-sound');
                }
            } else {
                updateLog("У вас недостаточно денег на терапию.", "warning");
            }
            
            processDayEnd();
        }
        
        function drinkEnergy() {
            gameState.money -= 200;
            gameState.fatigue = Math.max(0, gameState.fatigue - 20);
            gameState.mentalHealth -= 5;
            
            updateLog("Вы выпили энергетик. Усталость: -20. Психика: -5.", "warning");
            processDayEnd();
        }
        
        function declareBankruptcy() {
            const successChance = gameState.mentalHealth / 100;
            
            if (Math.random() < successChance) {
                // Успешное банкротство
                gameState.debt = 0;
                gameState.mentalHealth -= 30;
                gameState.relationship -= 30;
                gameState.collectorPressure = 0;
                
                updateLog("Вам удалось оформить банкротство. Долги списаны, но отношения разрушены.", "positive");
                playSound('money-sound');
            } else {
                // Неудачное банкротство
                gameState.debt = Math.floor(gameState.debt * 1.5);
                gameState.mentalHealth -= 40;
                gameState.collectorPressure = 100;
                
                updateLog("Попытка банкротства провалилась. Суд назначил дополнительные штрафы. Долг вырос.", "warning");
                playSound('negative-sound');
            }
            
            processDayEnd();
        }
        
        // Обновление коллекторов
        function updateCollectors() {
            // Увеличение давления каждые несколько дней
            if (gameState.day % 3 === 0 && gameState.debt > 0) {
                gameState.collectorPressure += Math.floor(gameState.debt / 500000);
            }
            
            // Звонок коллекторов
            if (gameState.daysToCollectorCall <= 0) {
                if (gameState.collectorPressure > 30 && Math.random() < 0.5) {
                    updateLog("Звонок от коллекторов. Они требуют немедленного погашения долга.", "warning");
                    gameState.mentalHealth -= 10;
                    gameState.daysToCollectorCall = Math.floor(Math.random() * 5) + 3;
                    playSound('negative-sound');
                }
            } else {
                gameState.daysToCollectorCall--;
            }
        }
        
        // Обновление неоновой вывески
        function updateNeonSign() {
            const sign = gameState.neonSigns[Math.floor(Math.random() * gameState.neonSigns.length)];
            document.getElementById('neon-sign-text').textContent = `"${sign}"`;
        }
        
        // Конец дня
        function processDayEnd() {
            gameState.day++;
            
            // Случайное событие
            if (Math.random() < 0.4) {
                triggerRandomEvent();
            }
            
            // Ежедневные изменения
            if (gameState.fatigue > 0) {
                gameState.fatigue = Math.max(0, gameState.fatigue - 5);
            }
            
            // Психика ухудшается от долга
            if (gameState.debt > 0) {
                const debtPressure = Math.min(5, Math.floor(gameState.debt / 200000));
                gameState.mentalHealth -= debtPressure;
            }
            
            // Отношения ухудшаются, если не уделять внимание
            if (gameState.day % 3 === 0 && gameState.relationship > 0) {
                gameState.relationship -= 2;
            }
            
            // Ограничение значений
            gameState.mentalHealth = Math.max(0, Math.min(100, gameState.mentalHealth));
            gameState.fatigue = Math.max(0, Math.min(100, gameState.fatigue));
            gameState.relationship = Math.max(0, Math.min(100, gameState.relationship));
            gameState.collectorPressure = Math.max(0, Math.min(100, gameState.collectorPressure));
            
            // Обновление неоновой вывески
            if (gameState.day % 2 === 0) {
                updateNeonSign();
            }
            
            // Обновление интерфейса
            updateUI();
            generateActions();
        }
        
        // Случайное событие
        function triggerRandomEvent() {
            const possibleEvents = gameState.randomEvents.filter(event => Math.random() < event.chance);
            
            if (possibleEvents.length > 0) {
                const event = possibleEvents[Math.floor(Math.random() * possibleEvents.length)];
                updateLog(event.message, "event");
                
                // Применение эффектов
                if (event.effect.money) gameState.money += event.effect.money;
                if (event.effect.mentalHealth) gameState.mentalHealth += event.effect.mentalHealth;
                if (event.effect.fatigue) gameState.fatigue += event.effect.fatigue;
                if (event.effect.relationship) gameState.relationship += event.effect.relationship;
                if (event.effect.collectorPressure) gameState.collectorPressure += event.effect.collectorPressure;
                
                if (event.effect.money && event.effect.money < 0) {
                    playSound('negative-sound');
                } else if (event.effect.money && event.effect.money > 0) {
                    playSound('money-sound');
                }
            }
        }
        
        // Проверка концовок
        function checkEndings() {
            for (const ending of gameState.endings) {
                if (ending.condition()) {
                    showEnding(ending);
                    break;
                }
            }
        }
        
        // Показать концовку
        function showEnding(ending) {
            gameState.isGameOver = true;
            
            // Заменить плейсхолдеры в тексте
            let text = ending.text;
            text = text.replace("{days}", gameState.day);
            text = text.replace("{debt}", formatMoney(gameState.debt));
            
            document.getElementById('ending-title').textContent = ending.title;
            document.getElementById('ending-text').textContent = text;
            document.getElementById('ending-screen').classList.add('active');
            
            // Остановить музыку
            document.getElementById('bg-music').pause();
            playSound('negative-sound');
        }
        
        // Перезапуск игры
        document.getElementById('restart-btn').addEventListener('click', () => {
            // Сброс состояния
            gameState.day = 1;
            gameState.money = 10000;
            gameState.debt = 1000000;
            gameState.fatigue = 30;
            gameState.mentalHealth = 50;
            gameState.relationship = 60;
            gameState.isGameOver = false;
            gameState.collectorPressure = 0;
            gameState.daysToCollectorCall = 5;
            gameState.neonBonus = 0;
            gameState.furnitureProgress = 0;
            gameState.furnitureActive = false;
            gameState.log = [];
            
            // Очистка лога
            document.getElementById('log-content').innerHTML = '';
            
            // Скрыть экран концовки
            document.getElementById('ending-screen').classList.remove('active');
            
            // Обновление интерфейса
            updateUI();
            generateActions();
            updateLog("День 1. Вы просыпаетесь в своей однокомнатной квартире. Рядом храпит ваша девушка. На столе лежат счета за коммуналку и напоминания о долгах.", "day");
            
            // Запустить музыку
            document.getElementById('bg-music').play();
            playSound('click-sound');
        });
        
        // Запуск игры при загрузке
        window.addEventListener('DOMContentLoaded', initGame);
    </script>
</body>
</html>
